node_modules
.next
out
build
*.db
*.db-journal
*.db-wal
*.db-shm
local.db*
# Environment: ignore EVERY .env variant except the committed template, so a real
# .env / .env.production / .env.*.local can never be accidentally committed.
.env
.env.*
!.env.example
uploads
.DS_Store
*.log
next-env.d.ts

# TypeScript incremental build info
tsconfig.tsbuildinfo
*.tsbuildinfo

# Local Claude Code settings (machine-specific; never shared).
.claude/settings.local.json
